/** Product copy — change `appTitle` to match reference (“Cardio-Pulse”) if needed. */
export const BRANDING = {
  appTitle: 'Readmission Research',
  appSubtitle: 'An AI-powered intelligence for precision quality metrics',
} as const;
