/** Default implementation for typecheck; Vite may alias to `readmissionDataset.parquet.impl.ts` at build. */
export {
  loadCaseIndex,
  loadCaseByRowId,
  clearDatasetCache,
} from '@/features/readmission/data/readmissionDataset.mock';
